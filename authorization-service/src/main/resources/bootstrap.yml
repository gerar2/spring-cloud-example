spring:
  application:
    name: authorization-service
  cloud:
    config:
      uri: http://localhost:8081

server.contextPath: /uaa
---

spring:
  profiles: cloud
  cloud:
    config:
      uri: http://br-config-service.cfapps.io/