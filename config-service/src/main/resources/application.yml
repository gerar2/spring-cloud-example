spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gerar2/spring-cloud-example
          search-paths: /configuration

server:
  port: 8081

